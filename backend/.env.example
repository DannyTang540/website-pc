# Railway MySQL Configuration
# Railway sẽ tự động inject các biến môi trường này khi bạn add MySQL service.
# File này chỉ là ví dụ, KHÔNG để mật khẩu thật ở đây.
MYSQLHOST=your_railway_mysql_host
MYSQLPORT=3306
MYSQLUSER=your_mysql_user
MYSQLPASSWORD=your_mysql_password
MYSQLDATABASE=railway

# Hoặc sử dụng DATABASE_URL (Railway cũng cung cấp). Ví dụ:
# DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here

# CORS Configuration
CLIENT_URL=https://website-pc-indol.vercel.app
ALLOWED_ORIGINS=https://website-pc-indol.vercel.app,http://localhost:3000

# Upload Configuration
UPLOAD_DIR=public/uploads
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp